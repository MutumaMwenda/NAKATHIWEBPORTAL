@model IEnumerable<NAKATHIWEBPORTAL.Models.Driver_ConductorVM>


<script src="~/global_assets/js/plugins/tables/datatables/datatables.min.js"></script>
<script src="~/global_assets/js/plugins/forms/selects/select2.min.js"></script>
<script src="~/global_assets/js/demo_pages/datatables_basic.js"></script>

<div class="navbar navbar-expand-lg navbar-light " style="margin-bottom:16px">

    <ul class="navbar-nav">
        <li class="nav-item">
            <a href="~/vehicle/driversConductor" class="navbar-nav-link  text-primary d-none d-md-block">
                <i class="icon-add"></i>
                Assign Driver/Conductor

            </a>
        </li>
    </ul>

    @*<ul class="navbar-nav">
        <li class="nav-item">
            <a href="#" class="navbar-nav-link  text-primary d-none d-md-block">
                <i class="icon-trash"></i>
                Delete Insurer
            </a>
        </li>
    </ul>*@



</div>





<!-- Basic datatable -->
<!-- /main navbar -->

<div class="card">

    <div class="card-header header-elements-inline">
        <h5 class="card-title text-center">Vehicle Assignment</h5>

        <div class="header-elements">
            <div class="list-icons">

            </div>
        </div>
    </div>



    <table class="table datatable-basic">
        <thead>
            <tr>
                <th>Id Number</th>
                <th>Reg NO</th>
                <th>Name</th>
                <th>Staff Type</th>
                <th>Route</th>
                <th>Date Assigned</th>
                <th>Assigned to Date</th>
                <th></th>
            </tr>
        </thead>
        <tbody>

            @foreach (var item in Model)
            {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.id_number)
                </td>
                
                <td>
                   
                    <a href="@Url.Action( "EditDriver",new { id = item.id })">@Html.DisplayFor(modelItem => item.reg_no)</a>

                </td>
                
                <td>

                    @Html.DisplayFor(modelItem => item.name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Sacco_Staff_Position_name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.route_name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.date_assigned)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.assigned_up_to_date)
                </td>

                @*<td>
                    @Html.ActionLink("Edit", "EditDriver", new { id = item.id }, new { @class = "editDriverModal", @data_taget = "editDriverModal" }) |
                    @Html.ActionLink("Details", "Details", new { id = item.id }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.id })
                </td>*@
            </tr>
            }

        </tbody>
    </table>
</div>







<script>

    var $alert = $('.alert');
    if ($alert.length) {
        setTimeout(function () {
            $alert.fadeIn(1000, 0);
            $alert.fadeOut("slow");
        }, 3000)

    }
</script>